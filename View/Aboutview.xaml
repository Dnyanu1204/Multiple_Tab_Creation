<UserControl x:Class="Multiple_Tab_Creation.View.Aboutview"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Multiple_Tab_Creation.View"
             mc:Ignorable="d" 
             xmlns:vm="clr-namespace:Multiple_Tab_Creation.ViewModel"
             Background="#303030"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <vm:AboutViewModel></vm:AboutViewModel>
    </UserControl.DataContext>
    <Grid>
        <Button Height="50" Width="200" Command="{Binding GetDataCommand }" Content="GetData"></Button>
    </Grid>
</UserControl>
